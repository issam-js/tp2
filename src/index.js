import React from "react";
import ReactDOM from "react-dom/client";
import SerieTP2 from "./SerieTP2";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <SerieTP2 />
  </React.StrictMode>
);
